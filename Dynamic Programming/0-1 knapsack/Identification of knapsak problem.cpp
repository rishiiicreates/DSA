
// input = wight[arr], value[arr], Capacity, lenght
// basicly, a single arry with two property, with weight and value property
// question can have single array, so we ignore value array if value property not given

// pattern: no of items + maximum capacity + choice

// initialization + DP = answer

